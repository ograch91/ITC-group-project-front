/**
 * show/search all users on DB adn start new chat with selected user
 * if conversation already exists, open it
 * support multiuser chat (with check box/some other way to select multiple users)
 */